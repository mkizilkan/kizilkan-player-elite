# TERMUX / GITHUB — v15.2.27-RC3

RC2 branch'i üzerinde yeni branch oluştur:

```bash
git switch -c v15.2.27-rc3
```

ZIP'i proje klasörünün üzerine açtıktan sonra:

```bash
git status --short
```

Beklenmeyen `D` yoksa:

```bash
git add -A
git commit -m "fix: KIZILKAN PLAYER ELITE v15.2.27 RC3 Stalker TS2345 type safety"
git push -u origin v15.2.27-rc3
```
