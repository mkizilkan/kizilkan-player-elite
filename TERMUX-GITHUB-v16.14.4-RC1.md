# v16.14.4 RC1 — GitHub Push

```bash
cd /sdcard/Download/kizilkan-player

git checkout -B v16.14.4-rc1-ci-hardening
git add -A
git status
git commit -m "KIZILKAN PLAYER v16.14.4 RC1 - CI hard-gate and MPV release-chain hardening"
git push -u origin v16.14.4-rc1-ci-hardening
```

Main branch'e otomatik merge yapılmaz.
