# KIZILKAN PLAYER v16.12.2 RC1 — TERMUX / GITHUB

Proje klasörü:

```bash
cd /sdcard/Download/kizilkan-player
```

Sürüm branch'i:

```bash
git checkout -b v16.12.2-rc1
```

Kontrol ve commit:

```bash
git status
git add -A
git status
git commit -m "KIZILKAN PLAYER v16.12.2 RC1 - PCAP-first rate-limit-aware MAG telemetry fix"
```

Yalnız bu sürüm branch'ini gönder:

```bash
git push -u origin v16.12.2-rc1
```

`main`, `gpt-dev` veya Claude branch'ine push yapılmaz.
