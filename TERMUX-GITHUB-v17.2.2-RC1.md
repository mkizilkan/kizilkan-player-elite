# TERMUX / GitHub — v17.2.2 RC1

Telefon yalnız hafif dosya/Git işlemleri için kullanılacaktır; Gradle/Expo build telefonda çalıştırılmayacaktır.

```bash
cd /sdcard/Download/kizilkan-player
unzip -o /sdcard/Download/KIZILKAN-PLAYER-v17.2.2-RC1-MPV-SURFACE-KOTLIN-CORRECTIVE-OVERLAY.zip -d /sdcard/Download/kizilkan-player

git status --short
git diff --stat
git diff --name-status
git diff --check
git diff -- frontend/src/utils/pin.ts frontend/app/profile-select.tsx
```

Diff temiz ve beklenen dosyalarla sınırlıysa commit/push yapılır.
