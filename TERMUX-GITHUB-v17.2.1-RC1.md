# TERMUX / GitHub — v17.2.1 RC1

Telefon yalnız hafif dosya ve Git işlemleri için kullanılacaktır; build/Gradle/Expo derlemesi telefonda çalıştırılmayacaktır.

```bash
cd /sdcard/Download/kizilkan-player
unzip -o /sdcard/Download/KIZILKAN-PLAYER-v17.2.1-RC1-TYPESCRIPT-SEMANTIC-CORRECTIVE-OVERLAY.zip -d /sdcard/Download/kizilkan-player
git status --short
git diff --stat
git diff --name-status
git diff --check
```

Diff doğrulandıktan sonra commit/push yapılır.
